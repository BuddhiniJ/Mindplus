# Soundscape Tracks

Mindplus now streams curated ambient music from Pixabay in
`src/config/soundscapeTracks.js`.

If you want to replace a track, swap its `uri` for another royalty-free MP3 URL
from a verified source and keep the `expo-av` audio format.
